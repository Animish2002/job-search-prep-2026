# Answers — Part 02 Code reading

## Snippet 1 — Java stream pipeline
Time taken: 
1. What it does:
2. Output for the given input:
3. Bug / edge case:
4. Fix / refactor:
Extra:

## Snippet 2 — Java synchronized
Time taken: 
1. What it does:
2. Trace of the scenario:
3. Bug / edge case:
4. Fix / refactor:
Extra (both true? refill twice? what C sees? is it a token bucket?):

## Snippet 3 — JS closures and this
Time taken: 
1. What it does:
2. Output / how many times `submit` runs, with what `this` and args:
3. Bug / edge case:
4. Fix / refactor and why it works:

## Snippet 4 — JS async race
Time taken: 
1. What it does:
2. What the user sees; what `init` logs:
3. Bug / edge case:
4. Fixes (counter, AbortController, loadAll):

## Snippet 5 — Java recursive tree walk
Time taken: 
1. What it does:
2. Trace of `path` and exact output:
3. Both defects:
4. Fix / refactor; the `remove(n.val)` trap:
